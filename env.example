# /opt/stacks/frigate/.env   (copy from this, fill real values, then: chmod 600 .env)
# gitignored — real values live in Proton Pass, never committed.
FRIGATE_MQTT_PASSWORD=changeme_mqtt
FRIGATE_RTSP_PASSWORD=changeme_reolink
